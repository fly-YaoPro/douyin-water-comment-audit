---
name: douyin-water-comment-audit
description: 抓取指定发布日期范围内的抖音达人视频公开评论，按既定水评规则识别问题视频，结合飞书客户表 AR 色块映射机构，生成“水评清理-时间”飞书表；默认 24 小时后复查同一批视频，生成机构处理对比并自动回到原内部调用群。用于“跑水评”“查水表”“水评清理”“抖音评论抽检”“检查达人评论区”“按机构统计水评达人”等任务。
---

# 抖音水评清理

## 强制前置门禁

1. 如果用户没有明确发布日期起止范围，先询问：
   `遥师，这次查询哪个发布日期范围？请给起止日期（例如 2026-08-01 至 2026-08-13）；也可以单独指定某个专项“全量”。`
2. 未取得时间范围前，不读取客户表、不启动爬虫。
3. 取得范围后，只执行只读预处理：导出源表、生成清单、统计待扫视频数。
4. 用 `scripts/estimate_duration.py` 给出预估耗时并等待确认。固定说明估算基准：
   - 2026-08-13 实测：1,207 条视频，用时 1 小时 20 分 34 秒。
   - 等价速度约 4.0 秒/条；同参数下预计时间允许上下浮动 20%。
   - 基准参数：并发 3、每视频最多抽取 100 条公开评论、20 条/批、复用登录态。
5. 明确列出：查询范围、工作任务条数、创作线条数、合计条数、预计耗时。等待用户明确回复“跑 / 开始 / 确认”后再启动爬虫。

不要把“用户提出任务”视为已经确认预估时间；时间范围和开跑确认是两个独立门禁。

## 执行流程

### 1. 读取规则与检查环境

- 完整读取 [references/workflow.md](references/workflow.md)。
- 使用飞书表格和飞书即时通讯能力时，遵守对应 `lark-sheets`、`lark-im` 与 `lark-shared` Skill。
- 先执行 `lark-cli auth status --json --verify`，复用已有用户授权；有效时禁止重复登录或要求扫码。
- 使用 `E:\豆包创作线Agent\MediaCrawler`。优先复用已保存的隔离浏览器登录态，不打开新的扫码窗口。
- 不调用 GLM 视觉。AR 机构通过 XLSX 单元格样式解析，不依赖截图识色。

### 2. 只读预处理与估时

按 [references/workflow.md](references/workflow.md) 中的源表配置导出目标 Sheet：

- CSV：读取 H/K/L/M/AR 的行数据。
- XLSX：读取 AR 的实际填色。

运行：

```powershell
python scripts/build_manifest.py `
  --work-csv <工作任务CSV> --work-xlsx <工作任务XLSX> `
  --creation-csv <创作线CSV> --creation-xlsx <创作线XLSX> `
  --start <YYYY-MM-DD> --end <YYYY-MM-DD> `
  --output <本次运行目录>\manifest.csv

python scripts/estimate_duration.py --manifest <本次运行目录>\manifest.csv
```

如用户指定某张表“全量”，为该表使用 `--work-all` 或 `--creation-all`。日期范围默认含首尾两天。

向用户报告待扫条数与预计耗时，等待第二次确认。

### 3. 抓取公开评论

确认后记录开始时间，运行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run_scan.ps1 `
  -Manifest <本次运行目录>\manifest.csv `
  -CrawlerRoot 'E:\豆包创作线Agent\MediaCrawler' `
  -OutputRoot <本次运行目录>\crawl `
  -BatchSize 20 -MaxComments 100 -CrawlerConcurrency 3 -Headless yes
```

- 持续监控，约每 5 批报告一次进度；不要每分钟刷屏。
- 每批独立落盘。失败批次必须重试或在结果中明确列出，不能静默跳过。
- 不抓子评论，不绕过平台访问控制，只处理公开评论。

### 4. 生成问题视频表

记录抓取结束时间并计算秒数，运行：

```powershell
python scripts/build_report.py `
  --manifest <本次运行目录>\manifest.csv `
  --data-root <本次运行目录>\crawl\data `
  --log-root <本次运行目录>\crawl\logs `
  --output-dir <本次运行目录>\report `
  --elapsed-seconds <实际秒数> --threshold 3
```

首轮报告创建时必须只有一个 Sheet：`问题视频`；列严格为：

`发布日期、达人昵称、发布链接、机构、专项、水评举例`

用 `lark-cli sheets +workbook-create` 创建 `水评清理-YYYY-MM-DD`；同日重跑则加 `-HHmm`。创建后回读验证：只有一个 Sheet、表头正确、数据行数与本地报告一致。

### 5. 先私聊确认，再发群

1. 用 bot 身份精确查找与“豆尼·银手”的 P2P 会话。
2. 私聊发送：表格链接、问题视频数、按机构去重后的水评达人数量、分析视频数、有效评论数、异常 JSONL 行数、实际用时、判定规则。
3. 等待用户明确确认。此时禁止发群。
4. 确认后用 bot 身份精确查找 `焦尾-豆包用增`，核对唯一匹配，再发送同一份摘要和链接。
5. 使用幂等键避免重复发送，并回报消息 ID 或成功状态。

### 6. 24 小时复查（默认开启）

- 首轮完成后固化本次 `manifest.csv`。24 小时复查时，必须重新读取两张原始客户发布表，并按首轮相同的发布日期范围重新生成清单；禁止把首轮结论 Sheet 当作复查数据源。
- 将本轮源表清单与首轮固化清单按 `aweme_id` 合并：本轮源表记录优先，首轮存在但本轮暂时缺失的视频仍保留复查。这样既能发现 24 小时内补登记的水评达人，也不会漏掉首轮对象。
- 复查结果追加到首轮工作簿的 `24h复查对比` Sheet，不改动 `问题视频` Sheet。
- 机构对比至少包含：首轮问题视频、可比较视频、已清理、仍有问题、无法复查、新增异常、处理率和结论。
- `已清理` 只能用于复查时确实成功读取、且不再命中问题规则的视频；抓取失败或无法读取一律计入`无法复查`，不得抬高处理率。
- 处理率分母为`可比较视频`，即首轮问题视频中复查成功的数量；样本过小时只称“本轮清理响应表现”，不下长期履约结论。
- 所有机构使用同一统计层级和同一结论格式，不区分“试点机构”与“其他机构”。机构逐项列出：已清理/首轮问题、处理率、仍有问题、新增异常、当前问题；没有足够样本时只陈述数据，不做排名。
- 对人输出应简洁、可读。先给总体数据，再按机构平铺结果，最后只补充影响理解的口径说明；禁止输出运行日志、内部字段名或冗长技术说明。
- 复查对比无需再次找遥师审批，自动发回原内部调用群；若原调用位置不是已登记的内部群或审批群，则禁止发送并保留待重试状态。
- 复查任务、Sheet 写入和群发送必须幂等；服务重启后从持久化任务恢复，不重新抓取已经完成的复查结果。

## 统计口径

- “分析视频数”：本次清单中的去重视频数；若抓取不完整，必须同时注明成功读取评论区的视频数。
- “评论区抽检数”：成功解析并按 `comment_id` 去重后的有效评论数。异常 JSONL 行另列，禁止从有效数里再次扣减。
- “水评达人数量”：问题视频对应的 `达人昵称`，按机构分别去重；不要把问题视频条数当达人数。
- 机构名使用“纽微特”，禁止写成“纽维特”。AR 写有“自建”时输出 `自建`，不归入任何机构。
- 问题视频判定：候选水评不少于 3 条，或同一账号发布 3 条相同/近似评论。

## 失败处理

- 源表颜色未知：标记 `未标注`，不要猜机构。
- 登录态失效：只有在实际验证失效后才请求一次最小必要登录；优先继续复用隔离配置。
- 短链无法解析、视频删除或评论关闭：记录为未成功读取，不伪造为空评论。
- 飞书表已创建但消息发送失败：保留表格并报告失败原因，重发消息时复用同一链接。
