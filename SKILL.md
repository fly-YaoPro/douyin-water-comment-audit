---
name: douyin-water-comment-audit
description: 抓取指定发布日期范围内工作任务、课程教学与独立端三类客户发布表中的抖音达人视频公开评论，按既定水评规则识别问题视频，并结合客户表机构色块映射机构，生成“水评清理-时间”飞书表；首轮、24 小时与 48 小时结果都回到原内部调用群并询问原发起人是否分发，确认后实时重读在线结果表，机构结果发对应机构群并艾特负责人，自建结果以同版卡片发“焦尾-豆包用增”内部群；24 小时卡片警告 48 小时未清完不结算，48 小时卡片正式告知对应任务不结算。用于“跑水评”“查水表”“水评清理”“抖音评论抽检”“检查达人评论区”“按机构统计水评达人”“把水评结果发机构”等任务。
---

# 抖音水评清理

## 公共业务配置

本 Skill 保留处理流程、例外和确认门禁，不单独维护客户表地址、周文档、机构色值或调度时间。先定位豆尼仓库（包含 `config/business-sources.json`），在仓库目录只读执行：

```powershell
node scripts/business-context.mjs --skill douyin-water-comment-audit
```

需要周执行文档时加 `--date YYYY-MM-DD`。阅读返回的来源、公共口径和本 Skill 写入边界；若运行时已注入 `runtime/business-context.json`，直接读取该快照，不另取一份不同版本。源缺失时停止相关操作，不能翻旧文档猜地址。手动和定时均调用原执行器，配置不授予新的写表或外发权限。

修改来源在仓库 `config/business-sources.json`，修改共用专项/色块/权威口径在 `config/business-policy.json`。调度以现有 Daemon 为准。脱离仓库独立打包时必须带上述配置和执行依赖、注明快照日期；不能把本 Skill 单独复制后视为可运行。Skill 自带命令仍在其原目录执行；包装器通过 `DOUNI_WORKSPACE` 或当前仓库目录定位执行器，不依赖固定盘符。

## 强制前置门禁

默认来源必须包含公共配置中的独立端发布页和对应审核机构映射，不因独立端单独维护工作簿而遗漏。未指定单表时，不能把聊天里最近提到的某张表擅自当作唯一来源。数据源登记、单次调用和实际读取是不同状态；来源读取失败必须明确指出表名，不能按0条生成完成结论。当前共享来源及后台定时可在豆尼工作台的调度台查看，登记本身不启动抓取或新增外发授权。

1. 先语义解析时间范围。完整起止日期，以及“8 月至今 / 本月 / 本周 / 上周 / 最近 N 天 / 昨天到今天”等边界明确的表达，都视为已提供范围；只有“最近 / 前阵子 / 这段时间”等边界不明确时才询问：
   `遥师，这次查询哪个发布日期范围？请给起止日期（例如 2026-08-01 至 2026-08-13）；也可以单独指定某个专项“全量”。`
2. 未取得时间范围前，不读取客户表、不启动爬虫。
   - 若当前消息或同一参数追问线程已经附带飞书工作簿链接、分享卡片或明确 Sheet 名，先把它作为本次数据源保留；后续补充日期时必须合并前后文，不得遗忘来源或切回默认客户表。
   - “查这个表格里 X Sheet，YYYY-MM-DD 至 YYYY-MM-DD”视为一条完整指令：打开该工作簿，读取 X Sheet 的整张 used range，再按日期字段筛选；不要把“这个表格”理解成固定客户表。
3. 取得范围后，只执行只读预处理：导出源表、生成清单、统计待扫视频数。
4. 用 `scripts/estimate_duration.py` 给出预估耗时并等待确认。固定说明估算基准：
   - 2026-08-13 实测：1,207 条视频，用时 1 小时 20 分 34 秒。
   - 等价速度约 4.0 秒/条；同参数下预计时间允许上下浮动 20%。
   - 历史估时基准参数为并发 3；当前生产抓取固定并发 1、每视频最多抽取 100 条公开评论、20 条/批、复用隔离登录态。并发降低用于减少抖音接口风控，实际耗时可能高于历史估算。
5. 明确列出：查询范围、实际数据源、实际 Sheet、待扫条数、预计耗时。默认三张客户发布表时分列工作任务、课程教学与独立端条数；用户指定单 Sheet 时只报告该 Sheet 的条数。等待用户明确回复“跑 / 开始 / 确认”后再启动爬虫。

时间范围明确后直接进入只读盘点与开跑确认，不再额外追问一次日期。不要把“用户提出任务”视为已经确认预估时间；时间范围和开跑确认仍是两个独立门禁。

## 执行流程

### 1. 读取规则与检查环境

- 完整读取 [references/workflow.md](references/workflow.md)。
- 当首轮结果生成、收到“是否转发”确认，或需要演练机构卡片时，完整读取 [references/institution-dispatch.md](references/institution-dispatch.md)。
- 使用飞书表格和飞书即时通讯能力时，遵守对应 `lark-sheets`、`lark-im` 与 `lark-shared` Skill。
- 先执行 `lark-cli auth status --json --verify`，复用已有用户授权；有效时禁止重复登录或要求扫码。
- 使用 `E:\豆尼·银手\MediaCrawler`。优先复用已保存的隔离浏览器登录态，不打开新的扫码窗口。
- 浏览器身份先同时检查 `HasUserLogin=1` 与 `sessionid/sessionid_ss`；`LOGIN_STATUS` 在当前抖音网页会话中不是必有字段，禁止把它作为硬门槛。真正可用性由首条作品详情接口与后续证据完整度门禁共同确认。
- 自动任务在无头模式发现登录失效时必须立即失败并提示重新登录，不得在后台等待二维码，也不得继续生成空结果。
- 不调用 GLM 视觉。工作任务与课程教学都从当前主发布表读取，先按“对应项目”列拆分专项，再从 AR 色块解析机构；旧课程发布表只补历史记录。独立端按达人昵称＋主页链接对应审核表，并从审核表 AS 色块解析机构；不依赖截图识色。

### 2. 只读预处理与估时

按 [references/workflow.md](references/workflow.md) 中的源表配置导出目标 Sheet：

- 当前主发布表：CSV 读取 B/H/K/L/M/AR。B 列“对应项目”含“工作任务”才进入工作任务；含“课程教学”或“3W 以下教学”统一进入课程教学；Excel 等其他项目不进入本次三类水评清单。XLSX 读取 AR 的实际填色。
- 旧课程发布表：仅作为历史补充源读取 H/K/L/M/AR，不再作为近期课程教学的唯一来源。主发布表与旧表按发布链接/GID 去重。
- 独立端：从`独立端-内容发布`按表头读取达人昵称、主页链接、发布日期、发布链接、GID；再用达人昵称＋主页链接对应`独立端-内容审核`，读取 AS 的机构色块。名称不是唯一键，只有昵称＋主页链接一致时优先匹配；昵称在审核表中只对应一个机构时才允许降级按昵称匹配。

若用户在当前消息或参数追问线程中明确附带其他飞书表格与 Sheet：

- 显式来源优先于固定源表。先用工作簿元数据核对真实 Sheet 名和稳定 Sheet ID，再导出该 Sheet 的完整 CSV/XLSX。
- 按真实表头匹配“机构 / 发布日期 / 达人昵称 / 视频（或发布链接）/ GID”等语义字段，禁止套用 H/K/L/M/AR 的固定列号。
- 日期兼容 `2026-08-17`、`8月17日`、`8.17` 等可无歧义归一的格式；请求范围提供年份时，月日值使用该年份。空白、文字误填或跨年歧义不得硬猜，计入无效日期并跳过/提示。
- 用户点名 Sheet 时读取整张 Sheet，再按日期范围过滤；不得只读屏幕可见的前几行。

运行：

```powershell
python scripts/build_manifest.py `
  --work-csv <工作任务CSV> --work-xlsx <工作任务XLSX> `
  --course-csv <课程教学CSV> --course-xlsx <课程教学XLSX> `
  --independent-csv <独立端发布CSV> `
  --independent-review-csv <独立端审核CSV> --independent-xlsx <独立端整簿XLSX> `
  --start <YYYY-MM-DD> --end <YYYY-MM-DD> `
  --output <本次运行目录>\manifest.csv

python scripts/estimate_duration.py --manifest <本次运行目录>\manifest.csv
```

用户指定单 Sheet 时，改用：

```powershell
python scripts/build_manifest.py `
  --custom-csv <指定Sheet CSV> --custom-xlsx <整簿XLSX> `
  --custom-sheet-name <真实Sheet名> `
  --start <YYYY-MM-DD> --end <YYYY-MM-DD> `
  --output <本次运行目录>\manifest.csv
```

如用户指定某张默认表“全量”，为该表使用 `--work-all`、`--course-all` 或 `--independent-all`。日期范围默认含首尾两天。

向用户报告待扫条数与预计耗时，等待第二次确认。

### 3. 抓取公开评论

确认后记录开始时间，运行：

```powershell
pwsh -ExecutionPolicy Bypass -File scripts/run_scan.ps1 `
  -Manifest <本次运行目录>\manifest.csv `
  -CrawlerRoot 'E:\豆尼·银手\MediaCrawler' `
  -OutputRoot <本次运行目录>\crawl `
  -BatchSize 20 -MaxComments 100 -CrawlerConcurrency 1 -Headless yes
```

- 持续监控，约每 5 批报告一次进度；不要每分钟刷屏。
- 每批独立落盘。失败批次必须重试或在结果中明确列出，不能静默跳过。
- 每批结束必须核对 `detail_contents` 证据行。若接口出现空响应/`DOUYIN_ACCESS_REJECTED` 且本批 0 条详情，立即熔断整单，不继续重试其余视频。
- 报告生成后、建飞书表前必须计算证据完整度：`成功取得详情或评论证据的视频数 ÷ 清单视频数`。为 0 或低于 80% 时整单失败，不建表、不发结论、不安排 24/48 小时复查。
- 不抓子评论，不绕过平台访问控制，只处理公开评论。

### 4. 生成问题视频表

记录抓取结束时间并计算秒数，运行：

```powershell
python scripts/build_report.py `
  --manifest <本次运行目录>\manifest.csv `
  --data-root <本次运行目录>\crawl\data `
  --log-root <本次运行目录>\crawl\logs `
  --output-dir <本次运行目录>\report `
  --elapsed-seconds <实际秒数> --threshold 3
```

首轮报告创建时必须只有一个 Sheet：`问题视频`；列严格为：

`发布日期、达人昵称、发布链接、机构、专项、水评举例`

用 `lark-cli sheets +workbook-create` 创建 `水评清理-YYYY-MM-DD`；同日重跑则加 `-HHmm`。创建后回读验证：只有一个 Sheet、表头正确、数据行数与本地报告一致。

### 5. 完成后回复原调用内部群，并询问是否分发

1. 首轮完成后直接回复原调用位置，内容包含：表格链接、问题视频数、按机构去重后的水评达人数量、分析视频数、有效评论数、异常 JSONL 行数、实际用时、判定规则。
2. 在同一条结果消息末尾原生艾特本次任务发起人，并询问：`是否将当前结果按机构转发？如需发送，请直接回复本消息“OK / 确认转发”。`
3. 原调用位置必须是已登记的内部群或审批群。结果本身不需要遥师二次复核；但向外部机构群发送属于新的外发动作，必须等原发起人对这条结果消息明确确认。
4. 保存结果消息、原发起人、运行记录、复查阶段和在线工作簿的绑定，支持每一轮独立确认分发和引用结论发起“复查”。
5. 使用幂等键避免重复发送，并回报消息 ID 或成功状态。

### 6. 原发起人确认后按机构分发

- 只认原发起人在 12 小时内直接回复该结果消息的肯定确认，包括“OK / 确认 / 确认转发 / 发机构”。其他人回复、脱离结果消息的孤立“OK”或过期确认均不触发外发。
- 收到首轮确认后，不使用本地 CSV、运行时缓存或确认前快照；必须立即重新读取在线工作簿的 `问题视频` Sheet，因为内部同事可能已经删减结果。
- 收到 24/48 小时复查确认后，同时重读`问题视频`与本轮对应的`24h复查对比`或`48h复查对比`，只发送`复查结果=仍有问题/新增异常`的视频；已清理、无法复查不发机构。水评举例优先沿用同一达人同一链接在`问题视频`中的原文；本轮新增异常无首轮举例时，明确写本轮候选数并引导查看复查表，不编造评论。
- 在线表头必须仍为`发布日期、达人昵称、发布链接、机构、专项、水评举例`。只保留`机构、达人昵称、发布链接、水评举例`四项都有内容的有效行；禁止把空行或残缺行发给机构。
- 按机构重新分组。没有有效结果的机构不发卡；`自建`使用与机构完全相同的表格卡片格式，固定发送到内部群`焦尾-豆包用增`，不艾特外部负责人；出现`未标注、金铲子`或其他未配置机构时停止该机构发送并回告发起人，禁止猜群。
- 每家使用 Card 2.0 宽表卡片，标题为`水评清理｜<机构>`，正文先原生艾特该机构负责人，再放三列表格：`达人昵称、视频链接、水评举例内容`。一行一个视频；链接显示为可点击的“查看视频”。24 小时卡片必须写明“首次检查满 48 小时仍未清完，对应任务不结算”；48 小时卡片必须写明“复查仍有水评，以下对应任务不结算”。
- 机构卡片只保留对应视频的发布链接，不得附首轮结果表、复查表或完整工作簿链接，避免外部机构申请内部原表权限。完整结果链接只允许留在原内部调用群。
- 仅此流程允许豆尼在固定机构群发送水评结果。该授权不扩大到读取机构群历史、回复机构其他消息或发送其他业务内容。固定群名、负责人和发送门禁见 [references/institution-dispatch.md](references/institution-dispatch.md)。
- 以`运行 runKey + 在线有效行快照哈希 + 机构`做幂等。逐机构记录消息 ID；部分失败时不得重发已成功机构，只向发起人列出成功、跳过和失败项。
- 确认后的快照发送完成即封版。若在线表之后再次变化，必须重新询问并获得一次新的明确确认。

### 7. 24 小时与 48 小时复查（默认开启）

- 首轮完成后固化本次 `manifest.csv` 和不可变数据源快照。24 小时复查时，必须重新读取首轮实际使用的原始来源：默认任务重读工作任务、课程教学、独立端三张客户发布表及独立端审核机构映射；用户指定单 Sheet 的任务重读同一工作簿、同一 Sheet。按首轮相同日期范围重新生成清单；禁止把首轮结论 Sheet 当作复查数据源，也禁止从自定义来源切回默认来源。
- 将本轮源表清单与首轮固化清单按 `aweme_id` 合并：本轮源表记录优先，首轮存在但本轮暂时缺失的视频仍保留复查。这样既能发现 24 小时内补登记的水评达人，也不会漏掉首轮对象。
- 24 小时复查结果追加到首轮工作簿的`24h复查对比`Sheet；48 小时复查结果追加到`48h复查对比`Sheet。两轮都不改动`问题视频`Sheet。
- `24h复查对比` 和后续人工复查 Sheet 必须在 H 列“复查结果”之后固定增加 I 列“人工复查”。I2 至数据末行使用单选下拉：`没水评`（蓝色 `#bacefd`）、`有水评`（橙色 `#fed4a4`）；禁止用自由文本替代。
- 机构对比至少包含：首轮问题视频、可比较视频、已清理、仍有问题、无法复查、新增异常、处理率和结论。
- `已清理` 只能用于复查时确实成功读取、且不再命中问题规则的视频；抓取失败或无法读取一律计入`无法复查`，不得抬高处理率。
- 处理率分母为`可比较视频`，即首轮问题视频中复查成功的数量；样本过小时只称“本轮清理响应表现”，不下长期履约结论。
- 所有机构使用同一统计层级和同一结论格式，不区分“试点机构”与“其他机构”。机构逐项列出：已清理/首轮问题、处理率、仍有问题、新增异常、当前问题；没有足够样本时只陈述数据，不做排名。
- 对人输出应简洁、可读。先给总体数据，再按机构平铺结果，最后只补充影响理解的口径说明；禁止输出运行日志、内部字段名或冗长技术说明。
- 两轮复查结果无需再次找遥师审批，自动发回原内部调用群，并在各自结果消息中原生艾特原发起人，询问是否发机构。每一轮都只认原发起人在该轮结果消息下 12 小时内的确认；首轮或 24 小时的确认不得沿用到下一轮。
- 24 小时确认外发时，机构卡片必须带一次结算警告；48 小时确认外发时，只发送当前仍有问题/新增异常的视频，并明确告知对应任务不结算。
- 24/48 小时复查任务、Sheet 写入和群发送分别幂等；服务重启后从持久化任务恢复，不重新抓取已经完成的复查结果，也不重复发送已成功机构。
- 复查表生成 2 小时后，自动回读同一工作簿、同一复查 Sheet。仅吸收“记录类型=视频明细”且“人工复查=没水评”的行，按`发布链接规范化值＋达人昵称规范化值`双字段写入豆尼本地长期记忆；两者必须同时命中才允许在后续检测中排除，禁止只按昵称或只按链接排除。
- 新一轮水评检测在估时和抓取前先应用这份长期记忆。命中的视频不进入扫描、问题数、机构统计或复查对比；回报中注明本轮因人工复查记忆排除的条数。人工标成“有水评”不进入排除库。

### 8. 引用结论发起人工复查

- 内部同事引用豆尼发出的首轮或复查结论，并单独说“复查”“再复查一下”等明确短指令时，先按被引用消息锁定原始运行与工作簿。
- 豆尼必须把复查规划回复给当前发起人；只有该发起人回复“OK”“确认”或“执行”后才启动，其他人的确认无效，12 小时后自动过期。
- 人工复查和自动复查使用首轮实际数据源与同一判定口径：重新读取原始来源，再与首轮固化清单合并，禁止从结论 Sheet 反推对象。
- 每次人工复查在原工作簿末尾新增唯一命名的复查 Sheet，不覆盖历史 Sheet。完成后直接回复当前已登记的内部群。

### 9. 长期水评台账

- 每次首轮检测和每次复查完成后，都把日期范围、结果表、核心统计、机构明细和本次结论追加到遥师指定的飞书文档。
- 台账写入使用稳定记录 ID 保证幂等；写入失败时保留加密载荷，每 15 分钟自动补写，不因群消息已发送而丢失结论。
- 台账用于后续分析机构执行效率；所有机构保持同一口径，结论必须保留样本量、已清理、仍有问题、新增异常和无法复查等必要上下文。

## 统计口径

- “分析视频数”：本次清单中的去重视频数；若抓取不完整，必须同时注明成功读取评论区的视频数。
- “评论区抽检数”：成功解析并按 `comment_id` 去重后的有效评论数。异常 JSONL 行另列，禁止从有效数里再次扣减。
- “水评达人数量”：问题视频对应的 `达人昵称`，按机构分别去重；不要把问题视频条数当达人数。
- 机构名使用“纽微特”，禁止写成“纽维特”。AR 写有“自建”时输出 `自建`，不归入任何机构。
- 问题视频判定：候选水评不少于 3 条，或同一账号发布 3 条相同/近似评论。

## 失败处理

- 源表颜色未知：标记 `未标注`，不要猜机构。
- 登录态失效：只有在实际验证失效后才请求一次最小必要登录；优先继续复用隔离配置。
- 登录恢复采用分级验证：可见浏览器重新登录后，先测 1 条，再测 5 条；两级均取得作品详情证据后才允许恢复全量任务。恢复期并发固定为 1。
- `DOUYIN_LOGIN_REQUIRED`、`DOUYIN_ACCESS_REJECTED` 和证据完整度不足属于不可盲重试错误；自动复查立即终止并留痕，不得每 15 分钟连续撞接口。
- 短链无法解析、视频删除或评论关闭：记录为未成功读取，不伪造为空评论。
- 飞书表已创建但消息发送失败：保留表格并报告失败原因，重发消息时复用同一链接。
